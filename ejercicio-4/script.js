/**
 * 8 divided by 4 is equals to 2.
 */

const divided = 8;
const divisor = 4;
const quotient = divided / divisor;
console.log(divided + " divided by " + divisor + " is equals to " + quotient + ".");